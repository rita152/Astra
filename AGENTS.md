# 项目背景与文档地图

本文件是仓库根级项目指引，适用于整个仓库。保持规则简短、具体、可执行；当前不使用子目录级 `AGENTS.md` 或 `AGENTS.override.md`。

## 项目背景

本项目是一个基于 Rust 与 GPUI 构建的原生桌面 Agent 应用，长期目标是为用户本机安装的各类 coding agent 提供统一入口，集中管理其发现、配置、启动、会话与运行状态。

项目目前处于协议接入阶段：只完成了 Codex app-server 部分生命周期、方法和事件的适配，尚未覆盖其全部协议能力；其他 coding agent 也还没有正式接入。后续扩展应以真实产品需求和各 agent 的协议边界为依据。

## 文档地图

仓库只维护以下 3 份 Markdown。

| 路径 | 类型 | 功能 | 何时更新 |
|---|---|---|---|
| `README.md` | 人工维护 | 项目定位、启动方式、核心验证命令、架构重构进度和文档入口 | 启动方式、依赖、常用命令或重构阶段变化时 |
| `AGENTS.md` | 人工维护 | 项目背景、仓库级工作约定与 Markdown 文件地图 | 项目定位、工作约定或文档职责变化时 |
| `docs/APP_SERVER_INTEGRATION.md` | 人工维护 | 当前 Codex app-server 全量 JSON-RPC 方法及接入状态的唯一总表 | Codex CLI 协议、运行时接入、passive 集合或兼容行为变化时 |

## Computer Use 界面验收

- 修改渲染或交互后，先构建最新可执行文件，再用独立 bundle ID 的 `GPUI Capture.app` 启动验收实例，避免占用或误操作 ChatGPT App 和用户正在运行的 GPUI 进程。
- 通过 Computer Use 先枚举应用并连接 `GPUI Capture`，再用可访问性树和截图定位目标；按问题需要执行滚动、点击、拖动或键盘操作，复现修复前行为并验证修复后行为。
- 前后验证使用相同主题、窗口尺寸、线程和滚动位置。交互问题必须检查完整命中区域、悬停/点击/键盘路径或文本选择等相关行为，不能只凭静态截图判定通过。
- 验收结束后仅关闭专用验收实例；同时运行与改动相称的格式、测试和编译检查，并在交付结果中记录实际验证范围与结果。

## 代码边界与重构

- `src/agent/` 的领域模块不得依赖 GPUI、界面组件或具体 agent 适配器；`mod.rs` 只维护模块与导出。
- Codex 的 wire 编解码留在 `src/agent/codex/`；连接生命周期与解码逻辑分开维护。通用媒体或文件工具不得经具体适配器向 UI 导出。
- 工作区状态留在 `src/workspace.rs`，分页加载和偏好持久化分别由 `src/workspace/loaders.rs`、`src/workspace/preferences.rs` 负责。
- 每批按职责迁移实现及对应测试；删除代码前核对调用方与协议覆盖，不通过扩大 `allow(dead_code)` 或 Clippy 抑制范围处理告警。
- 重构顺序、当前进度与验证入口维护在 `README.md` 的“架构与渐进重构”，不另建 Markdown 计划文档。
